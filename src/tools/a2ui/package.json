{
    "name": "discovery-a2ui",
    "version": "0.1.0",
    "private": true,
    "description": "Bundle A2UI (Agent-to-User Interface) renderer para o chat do Discovery. Gera a2ui-bundle.js (IIFE) commitado em frontend/.",
    "type": "module",
    "scripts": {
        "build": "node build.mjs",
        "build:watch": "node build.mjs --watch"
    },
    "dependencies": {
        "@a2ui/lit": "0.9.1",
        "@a2ui/web_core": "0.9.1"
    },
    "devDependencies": {
        "esbuild": "^0.24.0"
    }
}