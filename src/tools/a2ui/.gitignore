# Dependências npm do bundle A2UI — não commitadas.
# O artefato gerado (frontend/a2ui-bundle.js) é commitado; o runtime não
# depende de node/npm. Reinstale com `npm install` em src/tools/a2ui e rode
# `npm run build` apenas quando precisar regenerar o bundle.
# (tools/a2ui fica fora de frontend/ para não entrar no //go:embed — M19.)
node_modules/
package-lock.json